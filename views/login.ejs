<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" type="text/css" href="App_style.css" />
    <link href="https://fonts.googleapis.com/css?family=Noto+Sans&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Lobster|Poiret+One&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Raleway&display=swap" rel="stylesheet">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>

    <title>First APP</title>
    <style type="text/css">
        .message {
            font: 40px;
        }
    </style>

</head>
<body id="loginBody">
    <h1 class="loginHeader">Stock Investment Simulator</h1>

    <p class="pls">Please sign in.</p>

    <div id="loginForm">
        <form action="/login" method="POST">
            <input type="user" name="username" id="username" placeholder="Username">
            <br>
            <input type="password" name="password" id="password" placeholder="Password">
            <br>
            <% if (message != null && (message.logged_in != 'N/A' && message.logged_in != 'Password Sent')){ %>
                <div id="failMessage">
                    <span class="fail_mes" style="color: #fff">Username or password incorrect!</span>
                </div><br/>
            <% } else if(message.logged_in == 'Password Sent'){ %>
                <div id="failMessage">
                    <span class="fail_mes" style="color: #fff">Password Sent!</span>
                </div><br/>
            <% } %>
          <!--  <input type="submit" name="button" value="Sign in" id="loginButton">-->
            <button type="submit" id="loginButton">Sign In</button>
            <button type="submit" id="forgotButton"  formaction="/forgotpassword">Forgot Password</button>
            <!-- <button type="TEST" id="TEST"  formaction="/stockdata">TEST</button> -->
        </form>
        <br>
        <form action="/signup" method="GET">
            <button type="submit" id="signupButton">Sign Up</button>
        </form>
    </div>
   <!-- <% if (message.logged_in == 'true') { %>
    <div class="message"><%= message.success %></div>
    <% } else { %>
    <div class="message"><%= message.fail %></div>
    <% } %>-->
</body>

</html>
